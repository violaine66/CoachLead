<div class="container mt-5">
  <h1 class="text-center">Évaluations de pénibilité de l'activité professionnelle</h1>

  <% if current_user.entraineur? %>
    <div class="text-end my-4">
    <%= link_to "Nouvelle évaluation", new_job_load_evaluation_path, class: "btn-add text-white" %>
  </div>
  <% end %>
<% if @job_load_evaluations.present? %>
  <table class="table">
    <thead>
      <tr>
        <% if current_user.entraineur? %>
          <th>Joueur</th>
        <% end %>
        <th>Période</th>
        <th>Note</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      <% @job_load_evaluations.each do |evaluation| %>
        <tr>
          <% if current_user.entraineur? %>
            <td><%= evaluation.user.player_profil&.first_name %> <%= evaluation.user.player_profil&.last_name %></td>
          <% end %>
          <td><%= evaluation.period %></td>
          <td><%= evaluation.rating %></td>
          <td>
            <%= link_to "Voir", job_load_evaluation_path(evaluation), class: "btn btn-sm btn-primary text-white" %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>

<% else %>
  <p class="text-center">Pas de données pour le moment</p>
<% end %>

  <div class="my-3">
    <%= link_to "Retour", root_path, class: "btn btn-secondary text-white mt-3" %>
</div>
</div>
