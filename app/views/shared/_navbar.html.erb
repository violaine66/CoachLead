<div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
  <div class="container-fluid">
   <% if @player_profil.present? && @player_profil.persisted? %>
    <%= link_to player_profil_path(@player_profil), class: "navbar-brand" do %>
      <% if @player_profil.avatar.attached? %>
        <%= cl_image_tag @player_profil.avatar.key, class: "avatar-navbar", alt: "Avatar du joueur" %>
      <% else %>
        Mon Profil
      <% end %>
    <% end %>
  <% else %>
  <span class="navbar-brand">CoachLead</span>
  <% end %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto">
        <% if user_signed_in? %>
          <li><%= link_to "Se déconnecter", destroy_user_session_path, data: { turbo_method: :delete }, class: "dropdown-item text-dark" %></li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Se connecter", new_user_session_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
