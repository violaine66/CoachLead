<div class="container">
  <% if current_user.entraineur? %>
    <h1 class= "mt-5">Bienvenue Coach <%= current_user.pseudo %> !</h1>
    <%= link_to "Voir les joueurs", player_profils_path %>
  <% elsif current_user.joueur? %>
    <h1>Bienvenue <%= current_user.email %> !</h1>
    <p class="mt-3"><%= link_to "Mon profil", player_profil_path(current_user.player_profil) %></p>
  <% end %>
  <div class="entrainements mt-5">
    <h2><%= link_to "Mes Entrainements", trainings_path %></h2>
  </div>
  <% if current_user.entraineur?%>
    <div class="training-new">
      <h2><%= link_to "Créer un entraînement", new_training_path %></h2>
  </div>
  <% end %>
</div>
